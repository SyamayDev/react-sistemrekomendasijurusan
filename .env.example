# This file is an example. Create a .env.local or a .env file in the root directory
# and add your Gemini API key.
# For production, set this environment variable on your hosting platform (Vercel, Netlify, etc.).
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
